fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic-settings>=2.0.3
websockets>=11.0.3
numpy>=1.24.0

# STT (Whisper via Transformers) - PyTorch CUDA 12.9 for RTX 5060 Ti
# Install torch separately first:
#   pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu129
transformers @ git+https://github.com/huggingface/transformers.git
accelerate @ git+https://github.com/huggingface/accelerate.git
bitsandbytes
datasets
peft @ git+https://github.com/huggingface/peft.git
trl @ git+https://github.com/huggingface/trl.git

# LLM Agent
langgraph>=0.4.0
langchain-core>=0.3.0
httpx>=0.27.0

# Tool retriever (embedding-based tool selection)
sentence-transformers>=3.0.0

# Proactive agent scheduler
apscheduler>=3.10.0

# TTS (Kokoro - local, offline)
kokoro>=0.9.0
soundfile>=0.13.0

# Web search & scraping
ddgs>=8.0.0
readability-lxml>=0.8.0
lxml>=5.0.0

# Vision / Screen capture
mss>=9.0.0
Pillow>=10.0.0

# Cloud AI sub-agents
anthropic>=0.40.0
google-genai>=1.0.0
openai>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Short-term memory (Phase 1)
redis>=5.0.0,<6.0.0

# Profile (Phase 2)
asyncpg>=0.29.0

# Episodic (Phase 2)
qdrant-client>=1.12.0

# Auth (Google OAuth + signed cookies)
authlib>=1.7.0
itsdangerous>=2.2.0

# gRPC client for shore-ai-service
grpcio>=1.66.0
grpcio-tools>=1.66.0
protobuf>=5.27.0
